namespace ZombieSurvival
{
  /// <summary>
  /// The enum Mode determines, which window is currently open
  /// </summary>
  public enum Mode
  {
    Closing,
    Info,
    Score,
    Menu,
    Game
  }
}
